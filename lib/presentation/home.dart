import 'package:finserv/presentation/pending.dart';import 'package:finserv/presentation/received.dart';import 'package:flutter/material.dart';class Home extends StatefulWidget {  @override  _HomeState createState() => _HomeState();}class _HomeState extends State<Home> with SingleTickerProviderStateMixin {  TabController _tabController;  @override  void initState() {    _tabController = TabController(length: 2, vsync: this);    super.initState();  }  @override  Widget build(BuildContext context) {    return Scaffold(      backgroundColor: Colors.white,      appBar: AppBar(        backgroundColor: Colors.white,        elevation: 0,        leading: IconButton(            onPressed: () {              Navigator.pop(context);            },            icon: Icon(              Icons.arrow_back_ios,              color: Colors.black,            )),        title: Text(          'Finserv Home',          style: TextStyle(fontWeight: FontWeight.bold, color: Colors.black),        ),        bottom: TabBar(          controller: _tabController,          tabs: [            Padding(              padding: const EdgeInsets.all(10.0),              child: Center(                  child: Text(                'Pending EMI\'s',                style: TextStyle(                    fontWeight: FontWeight.bold,                    fontSize: 15,                    color: Colors.black),              )),            ),            Padding(              padding: const EdgeInsets.all(10.0),              child: Center(                  child: Text(                'Received EMI\'s',                style: TextStyle(                    fontWeight: FontWeight.bold,                    fontSize: 15,                    color: Colors.black),              )),            ),          ],        ),      ),      body: Container(        height: MediaQuery.of(context).size.height, // tab height        child: TabBarView(          controller: _tabController,          children: [Pending(), Received()],        ),      ),    );  }}